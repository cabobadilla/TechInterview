{
  "name": "tech-interview-analyzer",
  "version": "1.0.0",
  "description": "Tech Architecture Interview Analyzer",
  "main": "server.js",
  "scripts": {
    "start": "node server_new.js",
    "build-client": "cd client && npm install && npm run build",
    "render-postbuild": "mkdir -p client/build && mkdir -p uploads && npm run build-client && npm run migrate",
    "migrate": "node scripts/migrate_data.js",
    "db:setup": "node -e \"require('./database/config').initializeDatabase()\"",
    "verify": "node verify_deployment.js",
    "start:legacy": "node server.js"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^6.15.0",
    "jsonwebtoken": "^9.0.0",
    "openai": "^3.2.1",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3",
    "bcrypt": "^5.1.1",
    "google-auth-library": "^9.4.0",
    "uuid": "^9.0.1"
  }
} 